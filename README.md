# AI Investment Agent

This project is structured as a full-stack investment agent workspace containing:
- **`client/`**: React frontend initialized with Vite.
- **`server/`**: Node.js backend workspace.

## Getting Started

### Client Setup
To start the React frontend:
```bash
cd client
npm install
npm run dev
```
